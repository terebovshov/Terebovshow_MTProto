FROM ghcr.io/9seconds/mtg:latest
# Это официальный Docker-образ mtg, который уже скомпилирован
# для всех архитектур (x86, ARM, т.д.)
# Нам нужно только передать параметры запуска через CMD
CMD ["run", "--bind", "0.0.0.0:10000", "30a0360ee7f4b2fb3de3761b76534aed"]
